create table user (
   id bigint,
   name varchar,
   email varchar,
   registration_date timestamp
);

create table student(
    id integer,
    name varchar,
    age integer
);

create sequence hibernate_sequence;